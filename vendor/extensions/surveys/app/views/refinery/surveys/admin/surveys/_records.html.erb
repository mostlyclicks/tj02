<%= render 'refinery/admin/search_header', :url => refinery.surveys_admin_surveys_path %>
<div class='pagination_container'>
  <% if @surveys.any? %>
    <%= render 'surveys' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
